const init = {
    users: [
        {
            id: Math.random(),
            fullName: "Do Duong Thai Tuan",
        },
    ],
};

const reducer = (state = init, action) => {
    switch (action) {
        default:
            return state;
    }
};
export default reducer;
